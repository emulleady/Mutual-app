# Solo se usa para el build de producción (Render). En desarrollo local
# (npm run dev) dejalo vacío o no crees este archivo: se usa el proxy
# de Vite hacia http://localhost:4000 automáticamente.
VITE_API_URL=https://TU-BACKEND.onrender.com/api
